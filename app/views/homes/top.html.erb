<div class="container">
  <div class="row">
    <div class="col-xl-3"></div>
    <div class="text-center col-xl-6">
      <!--TOPページ-->
      <h1>welcome to <i class="fas fa-book"></i>Bookers !!</h1>

      <P>In <i class="fas fa-book"></i>Bookers,</P><br>
      <p>you can share and exchange your opinions , inpressions , and emotions</p><br>
      <p>about various books!</p>

      <% if user_signed_in? %>
       　<%= link_to "Log in", user_path(current_user.id), class: "btn btn-info btn-sm btn-block" %><br>
       　<%= link_to "Sign up", user_path(current_user.id), class: "btn btn-success btn-sm btn-block" %><br>
      <% else %>
      　 <%= link_to "Log in", new_user_session_path(@devise), class: "btn btn-info btn-sm btn-block"%><br>
       　<%= link_to "Sign up", new_user_registration_path(@devise), class: "btn btn-success btn-sm btn-block" %><br>
      <% end %>
    </div>
    <div class="col-xl-3"></div>
  </div>
</div>
